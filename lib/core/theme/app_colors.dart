import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color primaryDark = Color(0xFF0C1929);
  static const Color primaryAccent = Color(0xFF3A86FF);
  static const Color secondaryAccent = Color(0xFF5F0F23);
  static const Color background = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF6A6A6A);

  /// Amenity row (beds / baths / parking) on property cards.
  static const Color propertyCardAmenityAccent = Color(0xFF145A32);
}
